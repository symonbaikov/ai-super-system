// JSDoc typedefs for signals
/**
 * @typedef {Object} Candle {t:number,o:number,h:number,l:number,c:number,v:number}
 * @typedef {Object} Signal {
 *   kind: 'pump'|'dump'|'whale_in'|'entry'|'exit'|'pattern',
 *   t: number,
 *   price: number,
 *   strength: number, // 0..1
 *   meta?: any
 * }
 */
