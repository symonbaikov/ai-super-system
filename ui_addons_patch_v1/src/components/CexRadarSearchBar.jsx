import React,{useState}from"react";import{runCexSearch}from"../lib/cexRadarSearch";export default function CexRadarSearchBar({onResults}){const[q,setQ]=useState("");const[busy,setBusy]=useState(false);const go=async()=>{setBusy(true);try{const r=await runCexSearch(q);onResults?.(r);}finally{setBusy(false);}};return(<div className="flex items-center gap-2"><input className="w-full rounded-2xl bg-slate-800/80 border border-slate-700 px-3 py-2 text-slate-100" placeholder="Название или контракт" value={q} onChange={e=>setQ(e.target.value)}/><button onClick={go} disabled={!q||busy} className="px-3 py-2 rounded-xl bg-emerald-600 hover:bg-emerald-500 text-white text-sm">{busy?"...":"Пуск"}</button><button onClick={()=>{setQ("");onResults?.([]);}} className="px-3 py-2 rounded-xl bg-slate-800/60 border border-slate-700 text-slate-100 text-sm">Стоп</button></div>);}