import React,{useState}from"react";import{copyText}from"../lib/clipboard";export default function CopyMintButton({text="",title="Копировать"}){const[ok,setOk]=useState(false);const onCopy=async e=>{e.preventDefault();e.stopPropagation();const good=await copyText(text||"");setOk(good);setTimeout(()=>setOk(false),900);};return <button onClick={onCopy} title={ok?"Скопировано":title} className="inline-flex items-center justify-center w-6 h-6 rounded-md border border-slate-600 bg-slate-800/70 text-slate-200 hover:bg-slate-700">{ok?"✓":"⧉"}</button>}